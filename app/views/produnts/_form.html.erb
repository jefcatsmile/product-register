<%= form_with(model: produnt, local: true) do |form| %>
  <% if produnt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produnt.errors.count, "error") %> prohibited this produnt from being saved:</h2>

      <ul>
      <% produnt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :vender %>
    <%= form.text_field :vender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
